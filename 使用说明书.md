# 验收文档生成器 — 使用说明书

## 目录

1. [系统概述](#1-系统概述)
2. [快速入门](#2-快速入门)
3. [登录与用户管理](#3-登录与用户管理)
4. [LLM 配置](#4-llm-配置)
5. [文档生成（核心功能）](#5-文档生成)
   - [5.1 基本设置](#51-基本设置)
   - [5.2 参考文件](#52-参考文件)
   - [5.3 生成进度与章节列表](#53-生成进度与章节列表)
   - [5.4 预览与下载](#54-预览与下载)
   - [5.5 终止生成](#55-终止生成)
   - [5.6 章节重试](#56-章节重试)
   - [5.7 模板管理](#57-模板管理)
   - [5.8 如何生成高质量文档](#58-如何生成高质量文档)
6. [MD 转 Word](#6-md-转-word)
7. [文件管理](#7-文件管理)
8. [常见问题](#8-常见问题)

---

## 1. 系统概述

**验收文档生成器** 是一个基于大语言模型（LLM）的自动化文档生成工具。它可以根据您选择的文档类型和章节模板，自动生成符合国家标准的工程文档，并支持导出为 Word（.docx）格式。

**支持的文档类型（9 种）**：

| 文档类型 | 简称 | 说明 |
|---------|------|------|
| 需求规格说明书 | SRS | 描述软件的功能需求、非功能需求、接口需求等 |
| 概要设计文档 | HLD | 描述系统的总体架构、模块划分、技术选型等 |
| 详细设计文档 | DD | 描述模块内部实现、数据库设计、算法设计等 |
| 数据库设计文档 | DBD | 描述数据库表结构、索引、视图、安全策略等 |
| 测试计划 | TP | 描述测试资源、策略、进度、风险管理等 |
| 测试方案 | TS | 描述各项测试的具体方案和数据设计 |
| 测试用例 | TC | 描述功能、性能、安全等测试用例 |
| 测试记录 | TR | 记录测试执行情况和缺陷 |
| 测试报告 | TREP | 汇总测试结果，进行缺陷分析和评估 |

---

## 2. 快速入门

### 2.1 首次使用

1. **登录系统**：使用管理员分配的用户名和密码登录。
2. **配置 LLM**：点击左侧菜单"LLM 配置"，填写 API 地址、API Key 和模型名称，点击"测试连接"确认配置正确，再点击"保存配置"。
3. **开始生成**：进入"文档生成"页面，选择文档类型 → 选择章节模板 → 点击"开始生成文档"。

### 2.2 典型工作流

```
配置 LLM → 选择文档类型 → 选择/编辑模板 → 上传参考文件 → 填写写作要求 → 开始生成 → 预览 → 下载 Word
```

---

## 3. 登录与用户管理

### 3.1 登录

打开系统后，输入用户名和密码即可登录。系统会记住您的登录状态，关闭浏览器后需要重新登录。

### 3.2 修改密码

登录后，左侧菜单底部会显示您的用户名，点击"修改密码"按钮，输入原密码和新密码即可修改。

### 3.3 用户管理（仅管理员）

管理员用户可以在左侧菜单看到"用户管理"入口，可以进行以下操作：
- 创建新用户
- 删除用户
- 搜索用户
- 批量删除用户

---

## 4. LLM 配置

LLM 配置是使用系统的前提条件。每个用户可以配置自己的 LLM 连接信息，互不影响。

### 4.1 配置说明

| 配置项 | 说明 | 示例 |
|--------|------|------|
| **API 地址** | OpenAI 兼容 API 的地址 | `http://localhost:8000/v1` 或 `https://api.openai.com/v1` |
| **API Key** | 您的 API 密钥 | 输入后自动加密存储，保存后显示为脱敏的 `***` 格式 |
| **默认模型** | 要使用的模型名称 | `gpt-4`、`deepseek-chat` 等 |
| **全局写作要求** | 对所有文档生成生效的通用写作要求 | 例如"语言风格正式专业" |

### 4.2 配置来源

- **使用系统默认配置**：未填写个人配置时，使用系统管理员设置的全局配置
- **使用个人配置**：保存个人配置后，系统会优先使用您的个人配置

### 4.3 测试连接

保存配置前，建议先点击"测试连接"按钮，确认 LLM 服务可以正常访问。测试连接有 10 秒超时，如果配置错误会快速返回错误提示。

---

## 5. 文档生成

文档生成是系统的核心功能，页面分为**左侧配置区**和**右侧进度区**两个部分。

### 5.1 基本设置

#### 文档类型

系统支持 9 种文档类型（见[系统概述](#1-系统概述)）。选择文档类型后，系统会自动匹配对应的系统模板。

#### 章节模板

选择文档类型后，需要选择一个章节模板。模板分为两类：

- **系统模板**：系统预置的标准模板，基于 GB/T 8567-2006 国家标准设计。系统模板不可直接修改，编辑时会自动创建个人副本。
- **个人模板**：您自己创建或修改的模板，可以自由编辑和删除。

模板选择区域的四个按钮：
- **新建**：创建个人模板，设置名称、关联文档类型和描述
- **编辑**：打开章节模板编辑器，修改章节结构
- **删除**：删除个人模板（系统模板不可删除）
- 点击下拉框右侧的 **x** 可清除选择

#### 输出文件名

生成的文档保存时的文件名。可以不填，系统会自动使用"文档类型+时间"格式命名。

#### 写作要求

这是**影响生成质量最关键**的字段之一。在此输入您对文档的额外要求，这些要求会与 LLM 配置中的"全局写作要求"以及模板中各章节的"内容提示"合并，一起发送给大模型。

> **📝 写作要求填写建议详见 [5.8 如何生成高质量文档](#58-如何生成高质量文档)**

### 5.2 参考文件

参考文件是生成文档的重要依据。系统会解析上传的文件内容，并在生成每个章节时提供给大模型作为参考。

#### 支持的文件格式

`.docx`、`.pdf`、`.txt`、`.md`、`.xlsx`

#### 使用方法

1. 点击"上传文件"按钮，选择要上传的文件
2. 上传成功后，文件会出现在参考文件列表中
3. 在下拉框中勾选要使用的参考文件（可多选）
4. 选中的参考文件内容会随 prompt 一起发送给大模型

> **💡 提示**：参考文件内容较长时，系统会自动截取开头和结尾部分，确保不超过大模型的输入限制。建议参考文件保持精简，只包含与生成文档直接相关的内容。

#### 参考文件管理

上传的参考文件会保存在"文件管理"页面的"参考文件"标签页下，可以下载、编辑标签和备注、删除。

### 5.3 生成进度与章节列表

#### 开始生成

配置完成后，点击"开始生成文档"按钮，系统会：

1. **验证 LLM 连接**：快速检查 LLM 配置是否正确，如果配置错误会立即提示，不会浪费等待时间
2. **加载模板**：读取章节模板的章节结构
3. **逐章生成**：按照章节顺序，逐个调用大模型生成内容

#### 进度条

绿色进度条实时显示当前生成进度，同时显示文字提示（如"正在生成: 1.2 项目背景"）。

#### 章节列表

进度条下方显示所有章节的生成状态：

| 图标 | 状态 | 说明 |
|------|------|------|
| 🟢 绿色勾 | 已完成 | 该章节生成成功 |
| 🔵 蓝色旋转 | 生成中 | 正在生成该章节 |
| 🔴 红色叉 | 失败 | 该章节生成失败 |
| ⚪ 灰色时钟 | 等待中 | 该章节尚未开始生成 |

章节列表区域设置了**最大高度 400px**，章节过多时可以上下滚动，预览和下载按钮始终可见。

#### 章节状态标签

- **标题**：标记为灰色的章节，表示"仅生成标题"，不生成内容。这类章节在生成完成后不会显示"重新生成"按钮。
- **文字**：纯文字内容章节
- **表格**：纯表格内容章节
- **混合**：文字+表格混合内容章节

### 5.4 预览与下载

#### 文档预览

生成完成后，点击"预览"按钮，弹出预览窗口，包含：

- **左侧导航栏**：自动提取文档中所有标题（H1-H6），点击可快速跳转到对应位置。导航栏可折叠（点击 `◀` 按钮）。
- **右侧预览区**：渲染 Markdown 为 HTML，显示最终文档效果。滚动时导航栏会自动高亮当前所在章节。

#### 下载 Word 文档

点击"下载 Word 文档"按钮，系统会将生成的 Markdown 转换为 .docx 格式的 Word 文件并下载。下载的文件名即为"输出文件名"中设置的名称。

### 5.5 终止生成

如果在生成过程中想要停止，可以点击进度条右上角的"**终止生成**"按钮：

1. 系统弹出确认框，确认后终止生成
2. 终止后，该任务**不会保留**，所有已生成的内容将被清除
3. 页面状态回到初始状态，可以重新配置并开始生成

### 5.6 章节重试

如果某个章节的生成结果不满意，可以单独重新生成该章节：

1. 生成完成后，在章节列表中，已完成的章节右侧会出现"**重新生成**"按钮
2. 点击后弹出输入框，输入修改要求（如"内容不够详细，请加入更多数据示例"）
3. 系统会记住**所有历史修改要求**，并在重新生成时一并提供给大模型
4. 重试完成后，整个文档会自动重新组装

> **🔒 注意**：标记为"仅生成标题"的章节不会显示"重新生成"按钮。

#### 重试提示

- 重试时会保留前面章节的摘要信息，确保新生成的内容与上下文一致
- 每次重试的修改要求都会被累积，大模型会综合所有历史要求进行调整

### 5.7 模板管理

模板管理通过章节模板编辑器（ChapterTreeEditor）实现。

#### 创建模板

1. 在文档生成页面，点击模板选择器旁边的"新建"按钮
2. 填写模板名称、关联文档类型、描述
3. 点击"创建并编辑章节"，自动打开章节模板编辑器

#### 编辑模板

点击模板选择器旁边的"编辑"按钮，打开章节模板编辑器。

**⚠️ 重要**：编辑系统模板时，系统会自动创建一个**个人副本**，不会修改原始系统模板。编辑完成后会有提示"已创建个人副本并保存"。

#### 章节模板编辑器

编辑器分为左右两部分：

**左侧：章节树**

- **添加章节**：在根级别添加新章节
- **自动编号**：自动按"1. 2. 2.1. 2.2..."格式为所有章节添加编号
- **AI 分析**（仅个人模板可用）：上传文档，由 AI 自动分析文档内容并生成章节结构。详细说明见下文。
- **❓ 图标**：悬停查看 AI 分析功能说明
- **拖拽排序**：拖动章节节点可以调整顺序和层级
- **点击章节**：选中章节，右侧显示详情编辑面板

**右侧：章节详情编辑**

- **章节标题**：编辑章节的标题文字
- **仅生成标题**：开启后，该章节只输出标题，不调用大模型生成内容。适用于有子章节的父章节（如"第1章 引言"）。
- **内容类型**：选择章节的内容形式
  - **纯文字**：生成纯文字段落
  - **表格**：生成 Markdown 表格
  - **文字 + 表格**：先文字描述，再插入表格
- **内容提示**：告诉 AI 这个章节应该写什么内容。这是**每个章节级别的写作要求**，会与全局写作要求合并。
- **表格配置**（表格/混合类型）：设置表格行数、列数、表头，以及标记固定单元格内容
- **添加子章节**：在当前章节下添加子章节
- **添加同级章节**：添加与当前章节同级的章节
- **删除此章节**：删除当前选中的章节

#### AI 分析功能

AI 分析是一个强大的辅助工具，可以自动分析文档并生成章节结构。

**使用场景**：
- 您有一个需求文档（.docx），想快速生成对应的章节模板
- 您不确定某个文档应该包含哪些章节

**操作步骤**：
1. 点击"AI 分析"按钮
2. 选择要分析的文档文件（支持 .docx / .pdf / .txt / .md）
3. 如果编辑器中已有章节，系统会弹出确认框提示覆盖
4. 等待 AI 分析完成，章节树自动填充

**AI 分析的输出**：
- 为每个章节自动生成 `title`（标题）、`level`（层级，1/2/3）
- 自动填写 `content_prompt`（内容提示），说明该章节应该写什么
- 自动判断 `content_type`（纯文字/表格/混合）

> **⚠️ 注意**：AI 分析会**覆盖**当前章节结构。如果编辑器已有章节，请确认后再操作。AI 分析的结果是一个起点，您仍然可以手动调整。

### 5.8 如何生成高质量文档

生成高质量文档的关键在于**给大模型提供足够清晰的指导**。系统提供了三个层次的指导机制：

#### 第一层：全局写作要求（LLM 配置页）

这些要求会应用于**所有**文档生成任务。适合设置通用的写作风格要求。

**示例**：
```
语言风格正式专业，使用中文撰写。
内容详细具体，每个章节不少于 500 字。
涉及技术参数时使用表格呈现。
代码示例使用 Markdown 代码块格式。
```

#### 第二层：写作要求（文档生成页）

这些要求只对**当前这次**生成任务有效。适合设置针对特定文档的要求。

**示例**：
```
本次生成的需求规格说明书针对一个在线商城系统。
请重点描述用户注册、商品浏览、下单支付、订单管理四大模块。
系统后端使用 Java Spring Boot，前端使用 Vue.js，数据库使用 MySQL。
```

#### 第三层：内容提示（模板编辑器中的每个章节）

这是**最细粒度**的控制。为每个章节单独设置内容提示，告诉 AI 这个章节具体要写什么。

**示例**：

| 章节 | 内容提示 |
|------|----------|
| 1.1 编写目的 | 说明本文档为在线商城系统的需求规格说明书，面向开发团队和测试团队 |
| 2.1 产品描述 | 描述在线商城是一个 B2C 电商平台，支持商品浏览、搜索、购物车、下单、支付等核心功能 |
| 3.2 功能模块详述 | 逐一详细描述用户注册登录、商品管理、订单管理、支付管理四个模块的输入、处理和输出 |

#### 撰写内容提示的技巧

1. **具体明确**：不要写"描述系统功能"，要写"描述用户注册功能，包括注册流程、字段验证规则、密码加密方式"
2. **提供上下文**：告诉 AI 这是什么项目、什么系统，帮助 AI 生成更贴合实际的内容
3. **使用参考文件**：上传原始需求文档、设计文档等作为参考，AI 会从参考文件中提取信息
4. **表格章节**：对于表格章节，尽量在内容提示中给出表头列名，让 AI 生成规范的数据
5. **分层次**：父章节的内容提示可以概括，子章节的内容提示要具体

#### 完整示例：生成一份需求规格说明书

**步骤 1**：配置 LLM 全局写作要求
```
所有文档使用中文撰写，语言正式专业，内容详实。
使用 Markdown 格式，代码块注明语言类型。
```

**步骤 2**：选择文档类型"需求规格说明书"，使用系统模板

**步骤 3**：上传参考文件（如原始需求文档 .docx）

**步骤 4**：在"写作要求"中填写：
```
项目名称：在线商城系统（E-Shop）
项目背景：为某零售企业开发 B2C 电商平台，支持 PC 端和移动端
技术栈：后端 Spring Boot 3 + MyBatis，前端 Vue 3 + Element Plus，数据库 MySQL 8.0
核心功能：用户管理、商品管理、购物车、订单管理、支付集成、物流跟踪
系统用户包括：普通用户、商家、系统管理员
```

**步骤 5**：点击"开始生成文档"

**步骤 6**：针对不满意的章节，点击"重新生成"，输入修改要求

**步骤 7**：预览确认后下载 Word 文档

---

## 6. MD 转 Word

MD 转 Word 功能可以将 Markdown 文件转换为 Word 文档，无需调用大模型。

### 6.1 使用方式

提供两种方式输入 Markdown 内容：

**方式一：上传文件**
1. 将 .md 文件拖入上传区域或点击选择文件
2. 点击"转换并下载"按钮
3. 自动下载转换后的 .docx 文件

**方式二：粘贴内容**
1. 在文本框中粘贴 Markdown 内容
2. 输入输出文件名（可选，默认为"converted_document"）
3. 点击"转换并下载"按钮

### 6.2 转换历史

页面底部显示所有转换历史记录，可以：
- 下载历史转换的文档
- 删除单条记录
- 批量删除多条记录

> **🔒 注意**：MD 转 Word 的转换文件与系统生成的文档在文件管理中分开管理，互不影响。

---

## 7. 文件管理

文件管理页面分为两个标签页：

### 7.1 参考文件

管理在文档生成中上传的参考文件。支持：
- **搜索**：按文件名或标签搜索
- **筛选**：按文件类型（docx/pdf/txt/md/xlsx）筛选
- **上传**：直接上传参考文件
- **下载**：下载文件
- **编辑**：修改文件名、标签、备注
- **删除**：删除单个文件
- **批量删除**：选中多条记录后批量删除

### 7.2 生成文件

管理系统通过大模型生成的文档。支持搜索、筛选、下载、编辑、删除和批量删除。

---

## 8. 常见问题

### Q1: 点击"开始生成"后一直卡在"正在准备生成..."？

A: 系统在生成前会先测试 LLM 连接。如果 10 秒内无法连接，会显示错误提示。请检查：
- LLM 配置中的 API 地址是否正确
- API Key 是否有效
- 模型名称是否正确
- 网络是否能访问 LLM 服务

### Q2: 某个章节生成内容太少或不对？

A: 使用"重新生成"功能，在输入框中填写具体的修改要求。例如：
- "内容太少了，请扩展到 500 字以上"
- "请加入更多实际案例"
- "请使用更正式的措辞"

### Q3: 系统模板不满意，想修改？

A: 点击"编辑"按钮打开模板编辑器，修改后保存。系统会自动创建个人副本，不会影响系统模板。其他用户也不会看到您的修改。

### Q4: 如何让父章节（如"第1章 引言"）不生成内容？

A: 在模板编辑器中，选中父章节，开启"仅生成标题"开关。保存模板后，生成时该章节只输出标题，不调用大模型生成内容。

### Q5: 参考文件内容太长，会影响生成吗？

A: 系统会自动截取参考文件的开头和结尾部分，确保不超过大模型的输入限制。建议参考文件保持精简，只包含与生成文档直接相关的内容。如果文件很大，可以在上传前提取关键部分。

### Q6: 生成过程中可以切换页面吗？

A: 可以。生成任务在后台运行，切换到其他页面不会中断生成。切换回来时系统会自动恢复生成状态。但如果刷新页面，可能需要重新开始。

### Q7: 表格章节怎么设置？

A: 在模板编辑器中，将章节的"内容类型"设为"表格"或"文字 + 表格"，然后在"表格配置"中设置行列数。点击单元格可以标记为固定内容（预填固定的值），AI 会在生成时保留这些固定值，填充空单元格。

### Q8: 多个用户同时使用会冲突吗？

A: 不会。每个用户有独立的 LLM 配置、个人模板、参考文件。生成的任务也是独立的，互不影响。

### Q9: AI 分析生成的章节不满意怎么办？

A: AI 分析的结果是一个起点，您可以手动调整每个章节的标题、内容提示、类型等。也可以删除不需要的章节，添加新的章节。

### Q10: 如何让生成的文档更接近项目实际？

A: 三个关键点：
1. **上传参考文件**：上传项目的原始需求文档、设计文档等
2. **填写详细的写作要求**：在生成页面的"写作要求"中提供项目背景、技术栈、核心功能等信息
3. **编辑内容提示**：在模板编辑器中为每个章节填写具体的内容提示，指明要写什么