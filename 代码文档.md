# 验收文档生成器 — 代码文档说明

## 一、项目概述

验收文档生成器是一个基于 **FastAPI + Vue 3** 的 Web 应用，支持通过大模型（LLM）自动生成各类验收文档（需求规格说明书、概要设计、详细设计、测试计划等），并支持 Markdown 转 Word 导出。

### 技术栈

| 层级 | 技术 |
|------|------|
| 后端框架 | FastAPI + Uvicorn |
| 前端框架 | Vue 3 + Element Plus + Pinia |
| 数据库 | SQLite（异步 aiosqlite） |
| ORM | SQLAlchemy 2.0（异步） |
| LLM 调用 | httpx（OpenAI 兼容 API） |
| 认证 | JWT（pyjwt） |
| 文件解析 | python-docx / PyMuPDF / openpyxl / markdown |
| Word 生成 | python-docx |

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────┐
│                    浏览器                            │
│  Vue 3 + Element Plus + Pinia                       │
│  (编译后由 FastAPI StaticFiles 托管)                  │
└───────────────┬─────────────────────────────────────┘
                │ HTTP / WebSocket
┌───────────────▼─────────────────────────────────────┐
│                 FastAPI 后端                          │
│                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ routers  │  │ services │  │ middleware/auth  │  │
│  │ 路由层    │  │ 业务逻辑  │  │ JWT 认证中间件   │  │
│  └──────────┘  └──────────┘  └──────────────────┘  │
│                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ models   │  │ schemas  │  │ utils            │  │
│  │ 数据模型  │  │ 请求/响应 │  │ 工具函数          │  │
│  └──────────┘  └──────────┘  └──────────────────┘  │
└───────────────┬─────────────────────────────────────┘
                │
┌───────────────▼─────────────────────────────────────┐
│              SQLite 数据库                            │
│  User / DocumentTemplate / ChapterNode /             │
│  GenerationTask / ManagedFile / GlobalConfig         │
└─────────────────────────────────────────────────────┘
```

---

## 三、后端模块详解

### 3.1 入口文件 `backend/main.py`

```python
app = FastAPI(...)
# 1. CORS 中间件
# 2. JWT 认证中间件（AuthMiddleware）
# 3. 注册路由（auth, templates, files, generation, md2word, llm_config, admin）
# 4. 健康检查 GET /api/health
# 5. 挂载前端静态文件 frontend_dist/
```

启动命令：`uvicorn backend.main:app --host 0.0.0.0 --port 80`

### 3.2 配置 `backend/config.py`

基于 `pydantic_settings`，从 `.env` 文件加载配置。关键配置项：

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| `llm_base_url` | LLM API 地址 | `http://localhost:8000/v1` |
| `llm_api_key` | LLM API 密钥 | 空 |
| `llm_model` | 默认模型名称 | `default-model` |
| `app_secret_key` | JWT 签名密钥 | `change-this` |
| `admin_default_password` | admin 初始密码 | `admin123` |
| `database_url` | SQLite 数据库路径 | `storage/docs_gen.db` |
| `max_upload_size_mb` | 上传文件大小限制 | 50 MB |
| `generation_temperature` | LLM 生成温度 | 0.7 |
| `generation_max_tokens` | 单次最大 token | 4096 |
| `max_prompt_chars` | prompt 最大字符数 | 12000 |

### 3.3 数据库 `backend/database.py`

- 使用 `sqlalchemy` 异步引擎 + `aiosqlite`
- `init_db()` 在应用启动时创建所有表
- `get_db()` 为 FastAPI 依赖注入提供 session
- 首次启动时自动创建 admin 用户

### 3.4 数据模型 `backend/models/`

| 文件 | 模型 | 说明 |
|------|------|------|
| `user.py` | `User` | 用户表，含 LLM 配置字段（`llm_base_url`, `llm_api_key`, `llm_model`） |
| `template.py` | `DocumentTemplate` | 文档模板，含 `is_preset` 区分系统/个人模板 |
| `template.py` | `ChapterNode` | 章节节点，树形结构，含 `title`, `level`, `content_type`, `content_prompt` 等 |
| `document.py` | `GenerationTask` | 生成任务，含 `status`, `generated_md`, `chapter_results` 等 |
| `document.py` | `GlobalConfig` | 全局配置键值对 |
| `file.py` | `ManagedFile` | 文件管理，含 `category`（reference/generated）、`parsed_content` |

### 3.5 路由层 `backend/routers/`

| 文件 | 前缀 | 主要端点 |
|------|------|----------|
| `auth.py` | `/api/auth` | `POST /login` 登录；`POST /register` 注册；`GET /me` 当前用户信息 |
| `templates.py` | `/api/templates` | CRUD 模板；`POST /extract-word` 从 Word 提取目录；`POST /ai-analyze` AI 分析文档生成章节 |
| `files.py` | `/api/files` | 文件上传/下载/删除/列表/预览 |
| `generation.py` | `/api/generation` | `POST /start` 启动生成；`WS /{task_id}/progress` WebSocket 进度；`POST /{task_id}/retry-chapter` 重试章节；`POST /{task_id}/build-word` 构建 Word；`POST /{task_id}/cancel` 取消生成 |
| `md2word.py` | `/api/md2word` | Markdown 转 Word |
| `llm_config.py` | `/api/llm-config` | 用户 LLM 配置 CRUD；`POST /test-connection` 测试连接 |
| `admin.py` | `/api/admin` | 用户管理（列表/删除/批量删除/重置密码） |

### 3.6 业务逻辑 `backend/services/`

| 文件 | 说明 |
|------|------|
| `generation.py` | **核心**：`GenerationEngine` 类，逐章节调用 LLM 生成文档。支持续写、重试、取消，生成前自动检测 LLM 连接 |
| `llm_client.py` | `LLMClient` 类，封装 OpenAI 兼容 API 调用。支持非流式生成、流式生成、多轮对话、连接测试。提供 `get_llm_config_for_user()` 读取 per-user 配置 |
| `file_parser.py` | `FileParser` 类，解析 .docx / .pdf / .txt / .md / .xlsx 文件内容 |
| `word_builder.py` | `WordBuilder` 类，将 Markdown 转为 .docx 文件 |
| `md2word.py` | Markdown 到 Word 的转换逻辑 |
| `template_presets.py` | 预设模板定义，包含文档类型标签映射 |

### 3.7 工具函数 `backend/utils/`

| 文件 | 说明 |
|------|------|
| `chapter_tree.py` | 章节树操作：`flatten_tree()` 扁平化、`auto_number()` 自动编号、`count_chapters()` 计数 |
| `text_utils.py` | `extract_summary()` 提取章节摘要 |
| `auth.py` | JWT 生成与验证 |
| `logger.py` | 日志配置 |

### 3.8 认证中间件 `backend/middleware/auth.py`

`AuthMiddleware` 拦截所有 `/api/` 请求，验证 JWT Token（白名单路径除外：`/api/auth/login`, `/api/auth/register`, `/api/health`）。将解析出的 `user_id` 注入 `request.state.user_id`。

---

## 四、前端模块详解

### 4.1 页面结构

```
frontend/src/
├── main.js              # 入口，注册 Pinia/Router
├── App.vue              # 根组件，含导航栏和用户信息
├── router/index.js      # 路由配置，含登录守卫
├── api/
│   ├── index.js         # Axios 实例，请求/响应拦截器
│   ├── auth.js          # 认证相关 API
│   ├── templates.js     # 模板管理 API
│   ├── files.js         # 文件管理 API
│   ├── generation.js    # 文档生成 API
│   ├── md2word.js       # MD 转 Word API
│   └── admin.js         # 用户管理 API
├── stores/
│   └── generation.js    # 生成状态管理（WebSocket + 轮询）
├── views/
│   ├── Login.vue        # 登录页
│   ├── DocGeneration.vue # 文档生成页（主页面）
│   ├── FileManagement.vue # 文件管理页
│   ├── Md2Word.vue      # MD 转 Word 页
│   ├── LlmConfig.vue    # LLM 配置页
│   └── Admin.vue        # 用户管理页（仅 admin）
└── components/
    └── ChapterTreeEditor.vue # 章节模板编辑器（含 AI 分析）
```

### 4.2 关键页面说明

**DocGeneration.vue** — 文档生成主页面：
- 左侧：文档类型选择、模板选择（预设/个人）、写作要求、参考文件上传
- 右侧：生成进度（WebSocket 实时推送）、章节列表（可单独重试）、预览/下载按钮
- 支持终止生成、章节重试

**ChapterTreeEditor.vue** — 章节模板编辑器：
- 左侧树形章节结构，支持拖拽排序、添加/删除章节
- 右侧章节详情编辑（标题、内容类型、内容提示、表格配置）
- "AI 分析"按钮：上传文档自动生成章节结构（仅个人模板可见）
- 系统模板编辑时自动创建个人副本

**generation.js (Store)** — 生成状态管理：
- WebSocket 连接实时推送进度，5秒超时回退到 HTTP 轮询
- 持久化任务状态，页面切换后自动恢复

### 4.3 前端编译

```bash
cd frontend
npm run build
# 输出到 frontend/dist/
```

后端 `main.py` 中通过 `StaticFiles` 挂载 `frontend_dist/`，直接由 FastAPI 托管。

---

## 五、核心流程

### 5.1 文档生成流程

```
用户配置 → 选择模板/参考文件 → 点击"开始生成"
    ↓
POST /api/generation/start → 创建 GenerationTask → 后台启动 GenerationEngine
    ↓
WebSocket 连接 → 实时推送进度
    ↓
GenerationEngine.run():
  1. 加载模板章节树 → 首次推送章节列表
  2. 检测 LLM 连接（test_connection, 10s 超时）
  3. 逐章节构建 prompt → 调用 LLM → 解析结果
  4. 每章完成推送进度
  5. 组装 Markdown → 任务完成
    ↓
用户预览 → 下载 Word（POST /build-word）
```

### 5.2 AI 分析文档流程

```
用户点击"AI 分析" → 选择文件
    ↓
POST /api/templates/ai-analyze
  1. FileParser 解析文件为文本
  2. 检测 LLM 连接
  3. 构建 prompt → LLM 分析 → 返回章节 JSON
  4. 前端替换章节树
    ↓
用户可继续手动调整 → 保存模板
```

### 5.3 认证流程

```
用户登录 → POST /api/auth/login → 返回 JWT Token
    ↓
前端存储 Token → 每次请求自动携带（Authorization: Bearer xxx）
    ↓
AuthMiddleware 拦截 → 验证 Token → 注入 user_id
    ↓
各路由通过 request.state.user_id 获取当前用户
```

---

## 六、数据隔离

每个用户的数据通过 `owner_id` 字段隔离：

- **模板**：系统模板（`is_preset=true`）所有人可见，个人模板（`owner_id`）仅自己可见
- **文件**：上传文件通过 `owner_id` 隔离
- **生成任务**：通过 `owner_id` 隔离
- **LLM 配置**：存储在 `User` 表的 `llm_base_url`、`llm_api_key`、`llm_model` 字段中

---

## 七、API 端点总览

| 模块 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 认证 | POST | `/api/auth/login` | 登录 |
| 认证 | POST | `/api/auth/register` | 注册 |
| 认证 | GET | `/api/auth/me` | 获取当前用户 |
| 模板 | GET | `/api/templates` | 模板列表 |
| 模板 | POST | `/api/templates` | 创建模板 |
| 模板 | GET | `/api/templates/{id}` | 获取模板详情 |
| 模板 | PUT | `/api/templates/{id}` | 更新模板 |
| 模板 | DELETE | `/api/templates/{id}` | 删除模板 |
| 模板 | POST | `/api/templates/extract-word` | 从 Word 提取目录 |
| 模板 | POST | `/api/templates/ai-analyze` | AI 分析文档生成章节 |
| 文件 | GET | `/api/files` | 文件列表 |
| 文件 | POST | `/api/files/upload` | 上传文件 |
| 文件 | GET | `/api/files/{id}` | 获取文件详情 |
| 文件 | GET | `/api/files/{id}/download` | 下载文件 |
| 文件 | DELETE | `/api/files/{id}` | 删除文件 |
| 文件 | POST | `/api/files/batch-delete` | 批量删除文件 |
| 生成 | POST | `/api/generation/start` | 启动生成 |
| 生成 | WS | `/api/generation/{id}/progress` | WebSocket 进度 |
| 生成 | GET | `/api/generation/{id}` | 获取任务详情 |
| 生成 | GET | `/api/generation/{id}/preview` | 获取预览 |
| 生成 | POST | `/api/generation/{id}/retry-chapter/{ch}` | 重试章节 |
| 生成 | POST | `/api/generation/{id}/build-word` | 构建 Word |
| 生成 | POST | `/api/generation/{id}/cancel` | 取消生成 |
| 生成 | GET | `/api/generation` | 历史任务列表 |
| 生成 | DELETE | `/api/generation/{id}` | 删除任务 |
| MD 转 Word | POST | `/api/md2word/convert` | 转换 MD 为 Word |
| LLM 配置 | GET | `/api/llm-config` | 获取配置 |
| LLM 配置 | PUT | `/api/llm-config` | 更新配置 |
| LLM 配置 | POST | `/api/llm-config/test-connection` | 测试连接 |
| 管理 | GET | `/api/admin/users` | 用户列表 |
| 管理 | DELETE | `/api/admin/users/{id}` | 删除用户 |
| 管理 | POST | `/api/admin/users/batch-delete` | 批量删除用户 |
| 管理 | POST | `/api/admin/users/{id}/reset-password` | 重置密码 |
| 系统 | GET | `/api/health` | 健康检查 |

---

## 八、关键设计决策

1. **SQLite 数据库**：单文件部署，无需额外数据库服务，适合小规模团队使用
2. **Per-user LLM 配置**：每个用户可在前端配置自己的 LLM 地址和密钥，存储在 User 表中
3. **LLM 连接预检**：生成和 AI 分析前先快速检测连接（10s 超时），避免长时间等待
4. **系统模板保护**：编辑系统模板时自动创建个人副本，原模板不被修改
5. **WebSocket + HTTP 轮询双通道**：WebSocket 为主，5秒无响应自动回退到 HTTP 轮询，确保进度不丢失
6. **前端直接托管**：FastAPI 通过 `StaticFiles` 挂载前端编译产物，无需 Nginx 即可运行